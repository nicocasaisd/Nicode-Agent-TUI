# Coding Agent Python

Minimal CLI coding agent built with the OpenAI Responses API.

## Demo

> GitHub README rendering does not reliably autoplay or inline-play `.mkv` files.
> The snippet below is the closest README-compatible option, but playback support depends on the browser/GitHub renderer.

<video src="./demo.mkv" controls muted loop></video>

If the embedded player does not render on GitHub, use the direct file link instead:

[▶ Open `demo.mkv`](./demo.mkv)
